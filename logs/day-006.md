# Day 006

## Time
Reading: 1 hour
Coding: 2 hour

## HTTP

### What is an HTTP request?
Hyper Text Transfer Protocol is a set of rules to store and transfer data from a web app and a server.

### What is an HTTP response?
An HTTP response is the answer a server sends back to a client with information and a status code.

### What is an endpoint?
It is where the request from a client is executed.

## Endpoints I built

GET /products
app.MapGet("/products", () => {
    return products;
});

GET /products/{id}
app.MapGet("/products/{id}", (int id) =>
{
    Product? existingProduct = products.FirstOrDefault(product => product.Id == id);
    if(existingProduct == null){
        return Results.NotFound("The product doesn't exist");
    }
    else{
        return Results.Ok(existingProduct);
    }
});

POST /products
app.MapPost("/products", (Product newProduct) =>
{
    // your implementation
    if (string.IsNullOrWhiteSpace(newProduct.Name))
    {
        return Results.BadRequest("Name cannot be null, empty, or whitespace");
    }
    else if (newProduct.Quantity<0)
    {
        return Results.BadRequest("Quantity cannot be neggative");
    }
    else
    {
        int lastID = 0;
        foreach (var product in products)
        {
            if (product.Id>lastID)
            {
                lastID = product.Id;
            }
        }
        newProduct.Id = lastID + 1;
        products.Add(newProduct);
        return Results.Created();
    }
});

## HTTP status codes I used

200: The operation was successful.

201: Created. The resource was created successfully.

400: Bad Request. The client sent an invalid request.

404: Not Found. The resource doesn't exist.

## Request flow

Explain what happens when:

POST /products
HTTP sends the request -> POST /products JSON -> ASP.NET -> Transforms JSON -> Product newProduct -> 201 Created -> JSON -> Client

## C# → Web connection

What feels similar between a C# method and an API endpoint?
They feel like blocks of code with the same purpose. They receieve information, process it and return a result.

What feels different?
Syntax feels confusing.

## Bugs I encountered
I had trouble sending the request from the .http file

## What I understand better now
I now better understand the flow of the process when a client sends a request and how information returns to it.

## What still confuses me
Syntax and methods.